{
  "name": "auto-merge-action",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": ">=24.11.1 <25"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "scripts": {
    "build": "ncc build index.js -o dist"
  }
}
